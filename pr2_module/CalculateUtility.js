/**
 * create a Calculator Node.js Module with functions add, subtract,
 *  multiply, Divide. And use the Calculator module in another Node.js file.
 */

calculator = require('./Calculator');
calculator.add(1, 2);
calculator.sub(10, 5);
calculator.mult(10, 5);
calculator.div(24, 4);
